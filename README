Deployment:
  Requisites:
    git
      apt-get install git-core
    Apache
      apt-get install apache2
    Nginx
      apt-get install nginx
    php
      apt-get install php5 php5-mysql php-pear libapache2-mod-php5
    mysql
      apt-get install mysql-server
    libyaml
      apt-get install libyaml-dev
    phing
      pear channel-discover pear.phing.info
      pear install --alldeps phing/phing
    node.js
      git clone git://github.com/ry/node.git
      cd node
      ./configure
      make
      sudo make install
    lessc
      npm -g install less

  Steps:
    1. Copy repo to a directory readable to apache and nginx
    2. Run git submodule init
    3. Run git submodule update --recursive
    4. Enable apache2 mod_rewrite (a2enmod rewrite)
    5. Update config/config.ini with relevant information
    6. Run cd scripts/deploy && sudo phing
