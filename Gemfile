# frozen_string_literal: true

source 'https://rubygems.org'

# Run `bundle install` after making change here

gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-feed', '~> 0.12'
end
